# Example LLM Prompts

- "List workspaces and reports, then score modernization readiness for report `<id>` in workspace `<id>`."
- "Run dry-run style modernization on report `<id>` using `style_guide.enterprise.json` and summarize blockers/warnings."
- "Show field-level diff for proposed definition updates and identify high-risk changes."
